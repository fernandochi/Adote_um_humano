import styled from "styled-components";

export const EditDiv = styled.div`
  width: fit-content;
  position: relative;
  z-index: 1;
  bottom: 210px;
  left: 180px;
  border-radius: 50%;
  :hover {
    cursor: pointer;
  }
`;
