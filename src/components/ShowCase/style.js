import styled from "styled-components";

export const EditDiv = styled.div`
  width: fit-content;
  position: relative;
  bottom: 210px;
  left: 180px;
  border-radius: 50%;
  :hover {
    cursor: pointer;
  }
`;
